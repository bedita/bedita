build:
    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run

checks:
  php: true

filter:
  paths:
    - 'bedita-app/'
  dependency_paths:
    - 'vendor/'
    - 'bedita-app/vendor/'
  excluded_paths:
    - 'vendor/'
    - 'bedita-app/tmp/'
    - 'bedita-app/vendor/'