# <%= $plugin %> plugin for BEdita4

## Installation

You can install this plugin into your BEdita4 installation simply via clone or copy in `plugins/` folder.

Then run

```
composer update
```

To activate this plugin must be configured `Plugins` key, see https://docs.bedita.net/en/latest/configuration.html#plugins
